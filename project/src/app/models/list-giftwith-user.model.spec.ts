import { ListGiftwithUser } from './list-giftwith-user.model';

describe('ListGiftwithUser', () => {
  it('should create an instance', () => {
    expect(new ListGiftwithUser()).toBeTruthy();
  });
});
